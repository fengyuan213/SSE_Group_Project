# ============================================================================
# Environment Configuration Template
# ============================================================================
# Copy this file to .env and update with your actual credentials
# Command: cp env.example .env (automatically done during setup)
#
# NOTE: All variables in this file are automatically loaded:
#   - During container setup (post-setup.sh)
#   - In every new shell (zsh/bash auto-loads from .env)
#   - No need to manually export!
# ============================================================================


# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL Database Connection String
# Format: postgresql://username:password@host:port/database

# Local Docker Database (Development) - from docker-compose.yml
DATABASE_URL="postgresql://postgres:postgres@db:5432/sse_project"

# Remote Database (Production/Shared) - uncomment if using remote:
# DATABASE_URL="postgresql://admin:adminpassword@db.fengy.cc:6666/group_project"

# PostgreSQL Client Environment Variables (for psql command)
# These allow running 'psql' without connection arguments
# For local Docker database (from docker-compose.yml):
PGHOST=db
PGPORT=5432
PGUSER=postgres
PGDATABASE=sse_project
PGPASSWORD=postgres

# For remote database (uncomment and comment above when using remote):
# PGHOST=db.fengy.cc
# PGPORT=6666
# PGUSER=admin
# PGDATABASE=group_project
# PGPASSWORD=adminpassword


# ============================================================================
# AUTH0 CONFIGURATION (Future Use)
# ============================================================================
# Uncomment when implementing Auth0 authentication

# AUTH0_DOMAIN="your-tenant.auth0.com"
# AUTH0_CLIENT_ID="your_client_id"
# AUTH0_CLIENT_SECRET="your_client_secret"
# AUTH0_AUDIENCE="https://your-api-audience"

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

# Backend API Port (Flask development server)
# Default: 5000 (uncomment to change)
FLASK_PORT=5000

# Frontend Dev Server Port (Vite)
# Default: 5173 (uncomment to change)
VITE_PORT=5173

# Environment Mode
NODE_ENV=development

# CORS Origin for API (Frontend URL)
CORS_ORIGIN=http://localhost:5173

# ============================================================================
# PAYMENT INTEGRATION (Stripe)
# ============================================================================
# Get your Stripe keys from: https://dashboard.stripe.com/test/apikeys

# Stripe Secret Key (Backend) - Keep this secret!
STRIPE_SECRET_KEY=sk_test_mock_key

# Stripe Publishable Key (Frontend) - Safe to expose
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here

# ============================================================================
# GOOGLE MAPS API (for Geocoding & Maps)
# ============================================================================
# Get your API key from: https://console.cloud.google.com/

# Google Maps API Key (Frontend) - Restrict by HTTP referrer in Google Console
VITE_GOOGLE_MAPS_API_KEY=your_api_key_here

# ============================================================================
# EMAIL SERVICE (Resend)
# ============================================================================
# Get your API key from: https://resend.com/api-keys

# Resend API Key for sending confirmation emails
RESEND_API_KEY=re_your_key_here
RESEND_FROM_EMAIL=noreply@yourdomain.com

